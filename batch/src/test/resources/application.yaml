spring:
  datasource:
    url: jdbc:h2:mem:testdb;
    username: fiap
    password: fiap
  batch:
    jdbc:
      initialize-schema: always
      schema: classpath:schema-h2.sql

file:
  path: /Users/fabiomiyasato/Downloads/soccer-batch/arquivoparadeletar.txt
  resource: userlist.csv
